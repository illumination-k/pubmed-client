% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pubmed-client.R
\name{europepmc_search}
\alias{europepmc_search}
\title{Search Europe PMC}
\usage{
europepmc_search(client, query, limit = 20L, result_type = NULL, sort = NULL)
}
\arguments{
\item{client}{A \code{pubmed_client} created by \code{\link[=pubmed_client]{pubmed_client()}}.}

\item{query}{Europe PMC search query string.}

\item{limit}{Maximum number of records to return.}

\item{result_type}{Level of detail: \code{"idlist"}, \code{"lite"} (the default), or
\code{"core"}. \code{"core"} returns far more fields than are modelled; the
remainder is available in each record's \code{extra_json}.}

\item{sort}{Optional Europe PMC sort expression, e.g. \code{"P_PDATE_D desc"} for
newest first or \code{"CITED desc"} for most cited.}
}
\value{
A list of named lists, one per record, each with \code{id}, \code{source},
\code{europe_pmc_id}, \code{pmid}, \code{pmcid}, \code{doi}, \code{title}, \code{author_string},
\code{journal_title}, \code{pub_year}, \code{is_open_access}, and \code{extra_json}.
}
\description{
Searches every source Europe PMC indexes, following pages until \code{limit}
records are collected or the result set is exhausted. The query uses Europe
PMC's own syntax, e.g. \code{"TITLE:CRISPR AND SRC:PPR"}.
}
\examples{
\dontrun{
client <- pubmed_client(email = "you@example.com")
preprints <- europepmc_search(client, "TITLE:CRISPR AND SRC:PPR", limit = 5)
}
}
