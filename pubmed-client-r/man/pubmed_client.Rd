% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pubmed-client.R
\name{pubmed_client}
\alias{pubmed_client}
\title{Create a PubMed/PMC client}
\usage{
pubmed_client(
  api_key = NULL,
  email = NULL,
  tool = NULL,
  rate_limit = NULL,
  timeout_seconds = NULL
)
}
\arguments{
\item{api_key}{Optional NCBI API key.}

\item{email}{Optional contact email used to identify requests.}

\item{tool}{Optional tool name (defaults to the library default).}

\item{rate_limit}{Optional requests-per-second override.}

\item{timeout_seconds}{Optional HTTP request timeout in seconds.}
}
\value{
A \code{pubmed_client} object.
}
\description{
All arguments are optional. Supplying an NCBI \code{api_key} raises the rate limit
from 3 to 10 requests per second; \code{email} and \code{tool} are recommended by NCBI
for identification.
}
\examples{
\dontrun{
client <- pubmed_client(email = "you@example.com")
ids <- pubmed_search(client, "crispr", limit = 5)
}
}
