% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pubmed-client.R
\name{europepmc_database_links}
\alias{europepmc_database_links}
\title{List a Europe PMC record's external database cross-references}
\usage{
europepmc_database_links(client, id, source = NULL)
}
\arguments{
\item{client}{A \code{pubmed_client} created by \code{\link[=pubmed_client]{pubmed_client()}}.}

\item{id}{A record id, bare (\code{"PMC3258128"}, \code{"33515491"}) or fully
qualified (\code{"PPR/PPR123456"}).}

\item{source}{Optional source database: \code{"MED"}, \code{"PMC"}, \code{"PPR"}, \code{"AGR"},
\code{"CBA"}, \code{"PAT"}.}
}
\value{
A list of named lists, one per external database, each with
\code{db_name}, \code{db_count}, and an \code{info} list of cross-reference entries.
Europe PMC documents the four \code{info} slots only positionally, so they are
returned as \code{info1} to \code{info4} rather than renamed.
}
\description{
Europe PMC links records to external biological databases (UniProt, EMBL,
PDB, ChEBI, ArrayExpress, ...). A record with no cross-references returns an
empty list rather than an error.
}
