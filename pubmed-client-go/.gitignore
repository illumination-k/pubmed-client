# Static archives staged by `mise run go:build`. They are ~75 MB each, so they
# are built from source rather than committed.
lib/

# Binary left behind by a bare `go build ./examples/...`. The `go:test` task
# sends its output to a temp dir, so this only appears on a manual build.
/basic
