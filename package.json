{
  "name": "pubmed-client-wasm",
  "version": "0.1.0",
  "description": "WebAssembly bindings for the PubMed client library",
  "main": "pkg/pubmed_client_rs.js",
  "types": "pkg/pubmed_client_rs.d.ts",
  "files": [
    "pkg/"
  ],
  "scripts": {
    "build": "wasm-pack build --target nodejs --features wasm",
    "build:web": "wasm-pack build --target web --features wasm",
    "build:bundler": "wasm-pack build --target bundler --features wasm",
    "build:all": "pnpm run build && pnpm run build:web && pnpm run build:bundler",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:wasm": "wasm-pack test --node --features wasm",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check": "biome check --write .",
    "ci": "biome ci ."
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/illumination-k/pubmed-client-rs.git"
  },
  "keywords": [
    "pubmed",
    "biomedical",
    "research",
    "api",
    "wasm",
    "webassembly",
    "rust"
  ],
  "author": "Your Name",
  "license": "MIT OR Apache-2.0",
  "devDependencies": {
    "@biomejs/biome": "^2.0.6",
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^1.0.0",
    "happy-dom": "^12.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "wasm-pack": "^0.12.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
